# Data Notice

원본 데이터는 제공처 이용 조건상 저장소에 포함하지 않았습니다.

이 프로젝트의 코드는 아래 데이터 폴더 구조를 기준으로 정리합니다.

```text
data/
├── raw/
├── processed/
└── external/
```

데이터 접근이 가능한 환경에서는 동일한 파일명으로 데이터를 배치한 뒤 코드를 실행할 수 있습니다.
